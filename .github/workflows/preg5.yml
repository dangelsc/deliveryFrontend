name: preg 5
on:
  push:
    branches:
      - master
jobs:
  ping:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cc: [run1, run2, run3]
    steps:
      - name: trabajando
        run: |
          free -h
          mstat -s -S M
          ip ad

          
        
